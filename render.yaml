services:
  - type: web
    name: 3d-scan
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      # مفتاح KIRI Engine API — أدخله يدويًا من لوحة Render (لا يُحفظ في الكود أبدًا)
      - key: KIRI_API_KEY
        sync: false
